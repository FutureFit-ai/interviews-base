# interviews-base
Starting repos acting as a base for coding tech interview

Available starters:

- `node-coding` — TypeScript + Jest, no services
- `fullstack-coding` — React + Node/TS API + Postgres (Docker)
